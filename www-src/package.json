{
  "name": "www-src",
  "private": false,
  "version": "0.0.0",
  "description": "",
  "main": "ns-webview-bridge.js",
  "scripts": {
    "tslint": "tslint '*.ts' --config ../tslint.json --exclude \"**/*.d.ts\"",
    "format:check": "npm run format -- -c",
    "format": "prettier --write --config ../.prettierrc.json '*.ts'",
    "lint": "npm-run-all format:check tslint",
    "build": "tsc -p tsconfig.json"
  },
  "license": "MIT",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "prettier": "^1.19.1",
    "ts-node": "^8.3.0",
    "tslint": "^5.20.1",
    "typescript": "^3.7.3"
  }
}
